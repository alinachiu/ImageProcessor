package model.filter;

import model.IPhotoOperations;

/**
 * Represents a filter for an image as well as the operations it can perform (such as applying the
 * filter on an image).
 */
public interface IFilter extends IPhotoOperations {

}
